# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: PR Check

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  lint-and-publish:
    uses: kaiserbergin/schema-smith-actions/.github/workflows/schema-smith-lint-and-publish.yml@main
